# Releases

The project follows Semantic Versioning. Until 1.0, minor versions may contain documented breaking changes. Release tags are annotated and use the `vMAJOR.MINOR.PATCH` form. The release workflow publishes GitHub release notes only after CI has validated the exact commit; container publication is intentionally deferred until a registry and provenance policy are selected.
