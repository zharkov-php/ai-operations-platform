import { PageIntro } from "@/components/public-shell";
import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata("Security", "Security posture, data boundaries, responsible disclosure, and current limitations.", "/security");
export default function SecurityPage() { return <article className="section article"><PageIntro eyebrow="Security" title="Reduce sensitive data before it reaches storage" description="The security model combines tenant authorization, credential hygiene, pre-persistence redaction, safe logs, and explicit retention boundaries." /><section><h2>Data handling</h2><p>Raw prompt and response persistence is disabled by default. The ingestion boundary hashes prompts and creates redacted previews; known credential material and private-key markers are removed before persistence.</p></section><section><h2>Authentication boundaries</h2><p>Browser sessions use secure HTTP-only cookie semantics, mobile sessions use rotating refresh tokens, and ingestion uses scoped, hashed API keys with identifiable prefixes.</p></section><section><h2>Responsible disclosure</h2><p>Do not include credentials, private prompts, or personal data in a report. Follow the repository security policy for private reporting instructions once the public repository is available.</p></section><aside className="notice"><strong>Current limitation</strong><p>This is portfolio software, not an independently audited security product. A production deployment requires environment-specific threat modeling, secret management, monitoring, and review.</p></aside></article>; }
