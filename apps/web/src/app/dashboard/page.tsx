export const metadata = { robots: { index: false, follow: false } };

export default function DashboardFoundation() {
  return <main className="page-shell"><p className="eyebrow">Private platform</p><h1>Dashboard foundation</h1><section className="panel" aria-label="Implementation status">Authentication and analytics are planned for later phases. No example customer data is displayed.</section></main>;
}
